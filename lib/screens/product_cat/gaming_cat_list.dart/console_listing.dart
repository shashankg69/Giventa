import 'package:flutter/material.dart';
import 'package:giventa_app/models/product.dart';

class consoleList extends StatelessWidget {
  const consoleList({Key? key, required this.product}) : super(key: key);
  final Product product;
  static String routeName = "/gaming_cat_list";

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
